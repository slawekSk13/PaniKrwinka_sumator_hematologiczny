import styled from "styled-components";

const TipTextStyled = styled.p`
  text-align: center;
  font-size: 1.2rem;
  color: ${props => props.colors.accentColor};
  line-height: 1.3rem;
  padding: 2rem;
`;

export {TipTextStyled}